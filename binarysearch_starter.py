import csv
import string

# Open the CSV File and read it in.
f = open('countries.csv')
data = f.read()

# Split the data into an array of countries.
countries = data.split('\n')

length = len(countries)
print(countries[1])
print(countries)

# Start your search algorithm here.
pivotIndex = length/2
countries[pivotIndex] = pivot

while True:
    if user_input > pivot
        (((length - pivotIndex)/2) + pivotIndex) = pivotIndex
    elif user_input = pivot
        return user_input
        break
    elif user_input < pivot
        (pivotIndex)/2) = pivotIndex
